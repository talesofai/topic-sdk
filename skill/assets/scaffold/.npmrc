# topic-sdk 走 git 依赖（见 package.json），不从这里装。
# 此处 @talesofai scope 指向私有 registry（历史保留，便于将来引入其他 @talesofai 内部包）；
# 上传不再依赖 @talesofai/ali-oss-utils，deploy.mjs 直接用公共 npmjs 的 ali-oss。其余依赖走公共 npmjs。
@talesofai:registry=https://registry.npm.talesofai.cn/
