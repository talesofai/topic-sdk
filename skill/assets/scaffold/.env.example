# 复制为 .env（不要提交 git）。
#
# --- dev 模式（创作者发草稿，target=dev，默认）---
# 在 nieta-app 内"生成开发令牌"（账号设置 → 话题页开发者令牌），复制后填入。
# scoped 令牌：只能对自己有权限的话题发草稿，不能上线、不能做其它用户操作。
# 有效期 7 天，过期后重新生成。
NIETA_DEV_PUBLISH_TOKEN=<在 app 内生成的 dev 令牌>

# --- prod 模式（内部上线，target=prod，仅内部使用）---
# 必须是 is_internal 账号的完整登录态 token。创作者不需要填此项。
# NIETA_API_TOKEN=<内部运营 is_internal 账号的完整 token>

# --- 通用 ---
NIETA_ACTIVITY_UUID=<activity uuid，由平台分配给本话题>
NIETA_API_BASE=https://api.talesofai.cn
