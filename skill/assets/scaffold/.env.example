# 复制为 .env（不要提交 git）。凭证由创作者登录 nieta-app 取得自己的 API bearer token，
# 不得使用他人的 token；activity_uuid 由平台/运营分配。
# 上传不再静态持有任何 OSS 凭证：scripts/deploy.mjs 会用 NIETA_API_TOKEN 调
# GET /v1/oss/upload-grant 实时换取 scoped-to-prefix 的临时 STS 凭证。
NIETA_API_TOKEN=<creator/operator bearer token，从 nieta-app 登录态取得>
NIETA_ACTIVITY_UUID=<activity uuid，由平台/运营分配>
NIETA_API_BASE=https://api.talesofai.cn
