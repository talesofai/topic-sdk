# 内部上线专用（不要提交 git）。
# 必须是 is_internal 账号的完整登录态 token（deploy.mjs prod 模式用 x-token 头），
# 不得使用创作者的 token 或 scoped dev 令牌（NIETA_DEV_PUBLISH_TOKEN）。
# 上传不再静态持有任何 OSS 凭证：scripts/deploy.mjs 会用 NIETA_API_TOKEN 调
# GET /v1/oss/upload-grant 实时换取 scoped-to-prefix 的临时 STS 凭证（有效期 1h）。
NIETA_API_TOKEN=<内部运营 is_internal 账号的完整 token>
NIETA_ACTIVITY_UUID=<activity uuid，由平台分配>
NIETA_API_BASE=https://api.talesofai.cn
